# Block all direct web access to this folder. It only holds the
# rate-limit bookkeeping file for the contact form.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
