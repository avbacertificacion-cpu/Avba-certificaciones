# Block all direct web access to this folder. mail-config.php (server-only,
# never committed) lives here and must never be reachable by URL.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
